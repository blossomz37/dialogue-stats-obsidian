{
  "name": "dialogue-stats-obsidian",
  "version": "0.1.0",
  "description": "Obsidian plugin: word count and dialogue percentage for the active note.",
  "main": "main.js",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "tsc -p tsconfig.test.json && echo '{\"type\":\"commonjs\"}' > out/package.json && node --test test/*.test.cjs",
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "install:vault": "npm run build && mkdir -p ../basic-obsidian-vault/.obsidian/plugins/dialogue-stats && cp main.js manifest.json styles.css ../basic-obsidian-vault/.obsidian/plugins/dialogue-stats/",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "9.30.1",
    "@eslint/json": "^2.1.0",
    "@types/node": "^22.0.0",
    "esbuild": "0.25.5",
    "eslint": "9.39.1",
    "eslint-plugin-obsidianmd": "0.1.9",
    "globals": "14.0.0",
    "jiti": "2.6.1",
    "obsidian": "latest",
    "tslib": "2.4.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "8.35.1"
  }
}
